// Example: src/api.js   (create this helper or put in component)
const API_BASE = process.env.REACT_APP_API_URL;

// an axios instance
import axios from 'axios';
export const api = axios.create({
  baseURL: API_BASE,
  // you can add headers, timeouts, etc. here
});